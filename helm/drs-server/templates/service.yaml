apiVersion: v1
kind: Service
metadata:
  name: {{ include "drs-server.fullname" . }}
  labels:
    {{- include "drs-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drs-server.selectorLabels" . | nindent 4 }}

